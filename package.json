{
  "scripts": {
    "test": "NODE_ENV=test jest",
    "dev": "nodemon src/server.js --ignore *.spec.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "pg": "^8.7.3",
    "sequelize": "^6.20.0"
  },
  "devDependencies": {
    "jest": "^28.1.0",
    "nodemon": "^2.0.16",
    "sequelize-cli": "^6.4.1"
  }
}
